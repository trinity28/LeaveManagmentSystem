#features:- Register as an employee or a manager(localhost:3000/users/register login at localhost:3000/users/login

If logged in as employee:- :-will see history of your leave application at loalhost:3000,
>can create new leave application

If logged in as manager:->will see lsit of  all leave applications at loalhost:3000,
->can approve or reject the application

Proper validation check has been implemented both for leave and user register form.
